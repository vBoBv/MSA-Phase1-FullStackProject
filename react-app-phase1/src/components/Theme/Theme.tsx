import { createMuiTheme } from '@material-ui/core/styles';

const lightGrey = '#eef2f3';
const softOrange = '#FFA17F';

export default createMuiTheme({
	palette: {
		primary: {
			main: lightGrey
		},
		secondary: {
			main: softOrange
		}
	}
});
